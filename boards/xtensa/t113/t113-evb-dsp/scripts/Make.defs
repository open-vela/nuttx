############################################################################
# boards/xtensa/t113/t113-evb-dsp/scripts/Make.defs
#
# SPDX-License-Identifier: Apache-2.0
############################################################################

include $(TOPDIR)/.config
include $(TOPDIR)/tools/Config.mk
include $(TOPDIR)/arch/xtensa/src/t113/Toolchain.defs

# Board-specific linker script.

ARCHSCRIPT += $(call FINDSCRIPT,t113-evb-dsp.ld)

# We use the xtensa-hifi4-elf GCC's default endianness (little) and assemble
# with longcalls (already set in lx7 Toolchain.defs).  Nothing extra to add
# for Phase 1.

ARCHPICFLAGS = -fpic

CFLAGS    := $(ARCHCFLAGS) $(ARCHOPTIMIZATION) $(ARCHCPUFLAGS) $(ARCHINCLUDES) $(ARCHDEFINES) $(EXTRAFLAGS)
CPICFLAGS  = $(ARCHPICFLAGS) $(CFLAGS)
CXXFLAGS  := $(ARCHCXXFLAGS) $(ARCHOPTIMIZATION) $(ARCHCPUFLAGS) $(ARCHXXINCLUDES) $(ARCHDEFINES) $(EXTRAFLAGS)
CXXPICFLAGS = $(ARCHPICFLAGS) $(CXXFLAGS)
CPPFLAGS  := $(ARCHINCLUDES) $(ARCHDEFINES) $(EXTRAFLAGS)
AFLAGS    := $(CFLAGS) -D__ASSEMBLY__
